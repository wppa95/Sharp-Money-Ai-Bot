🔒 CREDENTIAL SAFETY — READ FIRST

Do not expose, print, log, echo, dump, or reveal any API keys, tokens, credentials, secrets, environment-variable values, or authorization data.

If checking Telegram delivery or timestamps, inspect only safe metadata such as:

* alert ID
* player/market
* sport
* alert_sent status
* Telegram delivery status
* message ID if safely available
* timestamps

Never output credential values.

━━━━━━━━━━━━━━━━━━

V3.2 POST-FREEZE CLARIFICATION / DISPLAY FIX

V3.2 Phase 2 Core is already frozen.

Do NOT reopen or modify:

* Scoring
* Qualification
* Tier thresholds
* Underdog pipeline
* Funnel logic
* Dashboard logic
* Database structure
* Telegram qualification behavior
* OddsAPI
* FanDuel
* DraftKings

We only need a small diagnostic/presentation clarification.

ISSUE 1 — /alerts COUNT CLARIFICATION

Current /alerts says something like:

🎯 Actionable Pick Alerts
8 shown · 11 all-time delivered

The user remembers receiving only approximately 1–2 actual Telegram actionable notifications today.

We need to determine exactly what these numbers mean.

Review the existing /alerts implementation and its data source.

DO NOT change alert qualification or delivery logic yet.

Determine:

1. Does “11 all-time delivered” mean:
    * 11 records where alert_sent=True,
    * 11 Telegram messages actually sent,
    * 11 messages successfully accepted by Telegram,
    * or something else?
2. Does /alerts include alerts from previous bot sessions/dates?
3. Does “today” refer to UTC date or the user’s local date?
4. Does the alert timestamp represent:
    * candidate creation,
    * alert creation,
    * Telegram send attempt,
    * Telegram successful delivery,
    * or another timestamp?
5. Can the system distinguish:
    * alert created
    * Telegram send attempted
    * Telegram send succeeded
    * Telegram send failed

If the current system does NOT track actual Telegram delivery separately, do NOT invent that information.

Instead, make the wording accurately describe what the database actually proves.

For example, if alert_sent=True only proves that the bot recorded the alert as sent, the UI should say something like:

“8 shown · 11 recorded as sent”

rather than implying confirmed Telegram delivery.

Do not change the underlying alert behavior in this pass.

━━━━━━━━━━━━━━━━━━

ISSUE 2 — STANDARD TIME DISPLAY

The bot is currently displaying times like:

17:05 UTC
13:57 UTC
13:26 UTC

The user wants standard 12-hour time with AM/PM.

Convert user-facing bot timestamps to:

* 5:05 PM
* 1:57 PM
* 1:26 PM

Do NOT change the underlying stored timestamps.

Do NOT change UTC storage.

Do NOT change scheduling.

Only change user-facing formatting.

Where a date is shown, preserve the date and use:

“Aug 08 · 5:05 PM”

instead of:

“Aug 08 17:05 UTC”

For user-facing Telegram/bot output, prefer 12-hour format with AM/PM.

Keep UTC available internally for logs/debugging where appropriate.

━━━━━━━━━━━━━━━━━━

TESTS

Add focused tests for:

* /alerts wording accurately describes what “delivered/sent” actually means.
* Historical alerts are clearly distinguishable from today’s alerts.
* UTC/local-date interpretation is not ambiguous.
* 12-hour formatting correctly converts:
    * 00:00 → 12:00 AM
    * 12:00 → 12:00 PM
    * 13:57 → 1:57 PM
    * 17:05 → 5:05 PM
    * 23:59 → 11:59 PM
* Stored timestamps remain unchanged.
* No credential values appear in output/tests.

Run focused tests and then the full test suite.

IMPORTANT

Do NOT modify the frozen V3.2 qualification/scoring/Underdog/Telegram delivery systems.

This is ONLY:

1. Clarify what /alerts counts.
2. Make user-facing times standard 12-hour AM/PM.

If investigation reveals that actual Telegram delivery cannot currently be proven from existing data, report that clearly instead of changing the alert pipeline.

━━━━━━━━━━━━━━━━━━

FINAL FREEZE

After the tests pass:

🧊 Keep V3.2 Phase 2 Core FROZEN.

Do not make any further changes beyond this display/diagnostic cleanup without approval.

Final report:

* Files changed
* Focused tests passed
* Full suite result
* Exact meaning of /alerts count
* Whether actual Telegram delivery can be confirmed
* Exact timestamp formatting change
* Confirmation stored timestamps were not changed
* Confirmation credentials remained protected
* Confirmation V3.2 qualification/scoring systems remain frozen