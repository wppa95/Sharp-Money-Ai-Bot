🔒 CREDENTIAL / SECRET SAFETY — READ FIRST

API keys, tokens, credentials, secrets, environment variables, authorization headers, and other sensitive values must NEVER be:

* Printed
* Logged
* Echoed
* Dumped
* Exposed in Telegram
* Shown in /status
* Shown in /health
* Shown in /config
* Included in diagnostics
* Included in test output
* Included in screenshots/reports

If credentials need to be verified, ONLY verify:

* Present / missing
* Connection works / fails
* Safe API response metadata

Never reveal the actual secret value.

The user will manually change secrets in Replit Secrets when necessary. Never ask the user to paste a secret into ChatGPT.

━━━━━━━━━━━━━━━━━━

V3.2 POST-FREEZE — ALERT CLARIFICATION + TIME DISPLAY + RESTART MEMORY

V3.2 Phase 2 Core is already frozen.

This is a small diagnostic/presentation/persistence cleanup.

Do NOT reopen or modify:

* Scoring engine
* Scoring thresholds
* Qualification thresholds
* Tier rules
* Underdog pipeline
* Funnel qualification logic
* Dashboard aggregation
* Database structure unless absolutely required for restart persistence
* Telegram qualification behavior
* OddsAPI
* FanDuel
* DraftKings

Do not make behavioral betting changes.

━━━━━━━━━━━━━━━━━━

ISSUE 1 — CLARIFY WHAT /alerts ACTUALLY COUNTS

Current output:

🎯 Actionable Pick Alerts
8 shown · 11 all-time delivered

The user remembers receiving only approximately 1–2 actual Telegram actionable notifications today.

Determine exactly what the number means.

Investigate the existing implementation and determine whether 11 all-time delivered represents:

1. Records where alert_sent=True
2. Telegram send attempts
3. Telegram sends successfully accepted by Telegram
4. Confirmed Telegram delivery
5. Something else

Also determine:

* Does /alerts include previous dates?
* Does “today” use UTC or the user’s local date?
* What timestamp does each alert display?
* Is that timestamp candidate creation, alert creation, send attempt, successful send, or another timestamp?
* Does the existing database store Telegram message IDs or delivery/send status?
* Can actual Telegram delivery currently be proven?

If the existing system cannot prove successful Telegram delivery, DO NOT invent a delivery confirmation mechanism.

Instead, change only the wording so it accurately describes what the database actually proves.

For example:

8 shown · 11 recorded as sent

may be more accurate than:

8 shown · 11 delivered

if the system only knows alert_sent=True.

Do NOT modify the actual alert qualification/delivery pipeline during this cleanup.

━━━━━━━━━━━━━━━━━━

ISSUE 2 — USER-FACING TIME FORMAT

The bot currently displays:

17:05 UTC
13:57 UTC
13:26 UTC

Change user-facing bot output to standard 12-hour AM/PM format.

Examples:

17:05 UTC → 5:05 PM
13:57 UTC → 1:57 PM
00:00 UTC → 12:00 AM
12:00 UTC → 12:00 PM
23:59 UTC → 11:59 PM

Where dates are displayed, use:

Aug 08 · 5:05 PM

Do NOT change:

* Stored timestamps
* Database timestamps
* UTC internal timestamps
* Scheduler timing
* Event timing
* Qualification timing

This is ONLY user-facing formatting.

Keep UTC available internally for debugging/logging where appropriate.

━━━━━━━━━━━━━━━━━━

ISSUE 3 — PERSISTENT BOT MEMORY / RESUME AFTER RESTART

IMPORTANT:

When the bot restarts, it must NOT behave like a brand-new bot.

The bot needs a reliable persistent memory/state mechanism so that when it comes back online it picks up where it left off instead of restarting its progress.

Use the existing database/persistent storage wherever possible.

A restart must preserve and restore important state, including:

* PropLineHistory
* Stored actionable picks
* Pending picks
* Resolved picks
* Historical scoring information
* Qualification history
* Pick lifecycle state
* Hit/Loss records
* Resolution state
* CLV seeds/history
* Alert history
* Any other important persistent intelligence/state already tracked by the bot

After restart:

Previous saved state
→ Bot starts
→ Load persistent state
→ Resume monitoring
→ Continue from existing progress

NOT:

Bot starts
→ Reset memory
→ Rebuild everything as if new

Important

Do NOT create a duplicate database or unnecessary new storage system.

Do NOT rewrite the architecture.

Do NOT reset existing records.

Do NOT delete historical data.

Do NOT reset scoring progress.

Do NOT reset PropLineHistory.

Do NOT reset pick history.

Do NOT reset qualification history.

Do NOT reset CLV state.

If the existing database already contains the required state, make the startup path correctly reload/use that state.

If any state is currently only stored in memory and would be lost on restart, identify it specifically.

Only add the minimum persistent-state mechanism required.

Restart verification

After implementation:

1. Record safe baseline state/counts.
2. Restart the bot.
3. Confirm the same database/state is loaded.
4. Confirm PropLineHistory count does not reset.
5. Confirm stored/pending pick counts do not reset.
6. Confirm alert history does not reset.
7. Confirm scoring/qualification history does not reset.
8. Confirm CLV state does not reset.
9. Confirm the bot resumes monitoring normally.
10. Confirm no duplicate alerts are generated simply because of the restart.

Do NOT count development restart events as lost bot memory.

The goal is:

Restart-safe persistent bot memory.

━━━━━━━━━━━━━━━━━━

TESTS

Add focused tests covering:

/alerts

* Wording accurately describes the underlying data.
* Historical alerts are distinguishable from today’s alerts.
* UTC/local-date handling is unambiguous.
* Actual delivery is not claimed unless provable.

Time formatting

* 00:00 → 12:00 AM
* 12:00 → 12:00 PM
* 13:57 → 1:57 PM
* 17:05 → 5:05 PM
* 23:59 → 11:59 PM
* Stored timestamps remain unchanged.

Restart persistence

* Existing database is reused.
* Existing PropLineHistory survives restart.
* Stored picks survive restart.
* Pending picks survive restart.
* Resolved picks survive restart.
* Qualification/scoring history survives restart.
* Alert history survives restart.
* CLV state survives restart.
* Restart does not create duplicate alerts.
* Startup resumes normal monitoring.

Security

* No credentials/secrets appear anywhere in output or tests.

Then run:

1. Focused tests
2. Full test suite

Do not proceed past a failed test.

━━━━━━━━━━━━━━━━━━

FINAL REPORT

Report:

* Files changed
* Focused tests passed
* Full test suite result
* Exact meaning of the /alerts count
* Whether actual Telegram delivery can be confirmed
* Exact time-format change
* Exact persistent-memory mechanism used
* What state is restored on startup
* Restart survival verification results
* Confirmation stored timestamps were untouched
* Confirmation credentials remained protected
* Confirmation scoring/qualification was untouched
* Confirmation Underdog pipeline was untouched
* Confirmation Telegram qualification behavior was untouched
* Any remaining known issues

━━━━━━━━━━━━━━━━━━

🧊 FINAL FREEZE

After the focused tests and full suite pass:

FREEZE V3.2.

Do NOT make additional changes after the freeze.

Do NOT:

* Add features
* Change thresholds
* Change scoring
* Change qualification
* Change Underdog behavior
* Enable sportsbook polling
* Enable FanDuel
* Enable DraftKings
* Change OddsAPI keys
* Start Phase 2 Sportsbook work

Any future change requires a separate approved task.

🧊 V3.2 — FROZEN