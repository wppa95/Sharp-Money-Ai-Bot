🔥 CREDENTIAL BURNING / COST WARNING — RESUME ONLY

The previous implementation attempt errored out mid-pass.

DO NOT restart the implementation from scratch and DO NOT redo completed edits.

Do not burn credentials/API quota with unnecessary scans or restarts.

Your job now is to RESUME FROM THE EXACT CURRENT CODEBASE STATE and finish the implementation safely.

━━━━━━━━━━━━━━━━━━━━━━━━
🔄 RESUME FROM AGENT ERROR
━━━━━━━━━━━━━━━━━━━━━━━━

The previous agent had already:

* Read the relevant codebase sections.
* Identified the current tier/direction rules.
* Began implementing the requested fixes.
* Modified market_engine.py.
* Modified alerts.py.
* Modified alerts_multiplatform.py.
* Began adding the L5 fallback.
* Began updating the S-tier/priority formatting.
* Began updating regression tests.
* Then encountered an agent error during the test-revert/regression-test phase.

DO NOT assume the edits are either complete or incomplete. Inspect the actual files first.

Step 1 — Inspect current state

Before changing anything:

1. Check git diff / current working-tree changes.
2. Read the modified sections of:
    * market_engine.py
    * alerts.py
    * alerts_multiplatform.py
    * relevant test files
3. Determine exactly which requested changes are already implemented.
4. Determine exactly which changes remain.
5. Do not overwrite completed work.
6. Preserve all valid changes from the previous agent.

Do not reset/revert the working tree.

━━━━━━━━━━━━━━━━━━━━━━━━
🎯 FINAL REQUIRED RULES
━━━━━━━━━━━━━━━━━━━━━━━━

TIER 2 — MLB/NFL

ACTIONABLE = S-tier OVER ONLY

S UNDER → WATCHLIST/BLOCKED
A OVER → WATCHLIST
A UNDER → WATCHLIST
B/C → WATCHLIST

No priority override, BQ, confidence, or alert path may bypass this.

TIER 1 — ALL OTHER SPORTS

ACTIONABLE = S/A OVER OR UNDER

B/C → WATCHLIST

Both OVER and UNDER are allowed.

BQ PRIORITY

BQ 80–100 is a strong-bet/priority signal only after the prop passes its sport/tier/direction eligibility gate.

BQ must NEVER override Tier 2’s S-tier OVER-only rule.

Strong UNDERs are allowed for Tier 1 when otherwise eligible.

━━━━━━━━━━━━━━━━━━━━━━━━
🔥 S-TIER PRIORITY OVERRIDE
━━━━━━━━━━━━━━━━━━━━━━━━

The priority override must use the same final eligibility decision as normal alert delivery.

It must NOT independently make a prop actionable.

Therefore:

Tier 2 MLB/NFL + S UNDER + BQ 95
→ BLOCKED/WATCHLIST

Tier 2 MLB/NFL + S OVER + BQ 95
→ ACTIONABLE PRIORITY

Tier 1 + S/A UNDER + BQ 80–100
→ ACTIONABLE STRONG BET where all normal gates pass.

━━━━━━━━━━━━━━━━━━━━━━━━
📊 L5 FALLBACK
━━━━━━━━━━━━━━━━━━━━━━━━

For ALL player props:

If normal historical data is unavailable:

* use available Underdog L5 data;
* display the last five results;
* clearly identify it as L5/fallback data;
* never fabricate results.

If neither source has usable history, report history unavailable.

━━━━━━━━━━━━━━━━━━━━━━━━
🔄 CONTINUOUS MONITORING
━━━━━━━━━━━━━━━━━━━━━━━━

Preserve the existing ScanCycleLog/full-feed monitoring implementation.

Every poll must monitor the complete active Underdog feed.

Known props must NOT be ignored permanently.

Stable lines may skip rescoring, but they must remain monitored.

Line changes and re-entry/reappearance must trigger rescoring when appropriate.

Keep the freshness timestamp on alerts:

⏱ Line as of [time] — verify on Underdog before placing.

━━━━━━━━━━━━━━━━━━━━━━━━
🔔 DELIVERY CONSISTENCY
━━━━━━━━━━━━━━━━━━━━━━━━

There must be ONE final eligibility decision used by:

* normal alerts
* line-change alerts
* re-entry alerts
* S-tier priority overrides
* Telegram delivery
* /picks delivery status

No secondary path may bypass the final gate.

━━━━━━━━━━━━━━━━━━━━━━━━
🧪 FINISH THE IMPLEMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━

After determining what the previous agent completed:

1. Finish all remaining requested edits.
2. Update only tests that enforce obsolete rules.
3. Add regression coverage for:
    * Tier 2 S OVER actionable
    * Tier 2 S UNDER blocked
    * Tier 2 A/B/C watchlist
    * Tier 1 S/A OVER actionable
    * Tier 1 S/A UNDER actionable
    * Tier 1 B/C watchlist
    * BQ 80–100 priority behavior
    * BQ cannot bypass Tier 2 direction
    * S-tier priority override uses final eligibility
    * L5 fallback
    * line freshness
    * continuous monitoring
    * delivery vs candidate status
4. Run syntax checks.
5. Run the full regression suite.
6. Fix failures caused by the implementation.
7. Run only the minimum live verification needed.
8. Restart the bot only if necessary.
9. Verify one normal scan cycle.
10. Verify /funnel, /picks, and /alerts.

Do NOT perform unnecessary API calls or repeated restarts.

━━━━━━━━━━━━━━━━━━━━━━━━
🚫 IMPORTANT
━━━━━━━━━━━━━━━━━━━━━━━━

Do NOT return another diagnosis-only report.

Do NOT say “I found the issue” and stop.

Do NOT ask me to manually finish edits.

Continue from the current state, finish the implementation, test it, and verify it.

If the previous agent left partially completed edits, build on them.

If a test reflects the OLD S-only/BQ-95/UNDER-blocking rules, update it to the FINAL rules above.

━━━━━━━━━━━━━━━━━━━━━━━━
🧊 FREEZE AFTER VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━

Once everything passes:

FREEZE THE BUILD.

No unrelated refactors.
No scoring changes.
No provider changes.
No new features.

Only confirmed bug fixes, stability/performance fixes, provider compatibility fixes, or explicitly approved rule changes after this point.

Final response must state:

✅ Previous work successfully resumed
✅ Remaining fixes completed
✅ Tests passed
✅ Live verification completed
✅ Tier 1 rules confirmed
✅ Tier 2 rules confirmed
✅ BQ priority confirmed
✅ L5 fallback confirmed
✅ Continuous monitoring confirmed
✅ Single final eligibility gate confirmed
🧊 BUILD FROZEN