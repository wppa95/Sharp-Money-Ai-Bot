🚨 URGENT V3.3 REGRESSION — FIX IN ONE PASS

⚠️ CREDENTIAL/CREDIT WARNING:
We just purchased additional credits.

DO NOT burn credentials unnecessarily.

Do NOT run broad rescans, repeated expensive polling, or unnecessary diagnostics.

Review the current live V3.3 code and fix the issue in ONE targeted pass.

━━━━━━━━━━━━━━━━━━
🚨 CONFIRMED LIVE BUG
━━━━━━━━━━━━━━━━━━

The bot is STILL sending this incorrect alert:

🔥🚨 S-TIER PRIORITY OVERRIDE — 52/100

MLB player
S-Tier
UNDER
Confidence: 95

Message says:

“⚡ Score ≥ 95/100 — immediate priority override.
All validation gates bypassed.”

This is WRONG.

We have now seen MULTIPLE versions of the same bad alert:

* Royce Lewis — Batter Strikeouts — UNDER — 52/100 — Confidence 95
* Lawrence Butler — Singles — UNDER — 52/100 — Confidence 95
* Tyler Soderstrom — Batter Strikeouts — UNDER — 52/100 — Confidence 95
* Jacob Wilson — Singles — UNDER — 52/100 — Confidence 95

This is both a qualification bug AND an alert-spam bug.

━━━━━━━━━━━━━━━━━━
🔥 CORRECT 95+ PRIORITY RULE
━━━━━━━━━━━━━━━━━━

95+ priority MUST use the ACTUAL CURRENT:

BET QUALITY / DECISION CONFIDENCE

NOT raw Sharp Money score.total.

Example:

Raw Score = 52/100
Bet Quality = 95/100

→ Evaluate as 95+ priority.

Example:

Raw Score = 95/100
Bet Quality = 70/100

→ NOT a 95+ priority.

The priority condition must therefore be based on the current decision’s Bet Quality/decision confidence field.

Do NOT use:

* score.total
* stale score
* stored score
* old snapshot
* tier label alone
* projected value

The current decision object must actually contain Bet Quality >= 95 at the moment the alert is created.

━━━━━━━━━━━━━━━━━━
🛡️ 95+ GATES
━━━━━━━━━━━━━━━━━━

A 95+ priority pick is NOT a complete bypass.

It must pass ONLY these two gates:

✅ Confidence Gate
✅ Quality Gate

Do NOT require:

❌ Secondary Evidence Gate
❌ Historical Sample Gate
❌ Other secondary qualification gates

Correct flow:

CURRENT BET QUALITY ≥ 95
→ Confidence Gate
→ Quality Gate
→ Sport Direction Policy
→ Existing Dedup/Reversal Protection
→ PRIORITY ALERT

━━━━━━━━━━━━━━━━━━
⚾ MLB / 🏈 NFL RULE
━━━━━━━━━━━━━━━━━━

MLB and NFL are Tier 2.

Tier 2 player props are:

🟢 OVER ONLY

Therefore:

MLB/NFL OVER + current BQ 95

* Confidence Gate
* Quality Gate

→ 🔥 PRIORITY

MLB/NFL UNDER

→ ❌ BLOCKED

The 95+ priority rule MUST NEVER bypass this direction rule.

This is exactly why the recent Royce Lewis / Lawrence Butler / Tyler Soderstrom / Jacob Wilson UNDER alerts are invalid.

━━━━━━━━━━━━━━━━━━
🎯 TIER 1 RULE
━━━━━━━━━━━━━━━━━━

Tier 1 = every monitored sport other than MLB/NFL.

Tier 1 supports BOTH:

🟢 OVER
🔴 UNDER

Tier 1 OVER + BQ 95

* Confidence Gate
* Quality Gate

→ 🔥 PRIORITY

Tier 1 UNDER + BQ 95

* Confidence Gate
* Quality Gate

→ 🔥 PRIORITY

Do NOT make Tier 1 OVER-only.

Do NOT force Tier 1 to follow the MLB/NFL UNDER restriction.

Evaluate each sport independently.

Do not compare one sport’s score distribution against another sport’s distribution.

━━━━━━━━━━━━━━━━━━
⚡ 90–94 RULE
━━━━━━━━━━━━━━━━━━

90–94 Bet Quality:

→ ⚡ HIGH PRIORITY

But 90–94 does NOT receive the 95+ priority override.

90–94 must continue through the normal applicable qualification gates.

Therefore:

95–100 BQ
→ Confidence + Quality
→ Sport Direction
→ Dedup/Reversal
→ Priority

90–94 BQ
→ Normal applicable gates
→ Sport Direction
→ Dedup/Reversal
→ High Priority

━━━━━━━━━━━━━━━━━━
🔔 CRITICAL SPAM FIX
━━━━━━━━━━━━━━━━━━

The recent four MLB UNDER alerts prove the priority system is capable of spamming Telegram.

95+ priority must NOT mean unlimited alerts.

Use the existing alert memory/dedup/reversal protection.

Before sending a priority alert:

✅ Provider
✅ Player
✅ Market
✅ Game/Event
✅ Recent alert history
✅ Duplicate protection
✅ Reversal/conflicting alert protection

Do NOT repeatedly send the same player/market/game opportunity during rescans.

Do NOT send conflicting reversals repeatedly.

A genuinely NEW opportunity must still be allowed later.

Do NOT solve spam by disabling priority.

Do NOT solve spam by lowering the 95 threshold.

Use the existing lightweight protection.

━━━━━━━━━━━━━━━━━━
📨 TELEGRAM MESSAGE FIX
━━━━━━━━━━━━━━━━━━

The alert wording itself is currently misleading.

STOP displaying:

“Score ≥ 95/100 — immediate priority override.
All validation gates bypassed.”

Replace it with something accurate, such as:

“🔥 Bet Quality 95/100 — Priority”

And only display the priority label when the CURRENT Bet Quality is actually 95+ and the required gates have passed.

Do NOT call it a score of 95 when the raw Sharp Money score is 52.

━━━━━━━━━━━━━━━━━━
📅 /PICKS FIX
━━━━━━━━━━━━━━━━━━

/picks must show the CURRENT-DAY SLATE.

It should NOT show a 72-hour window.

Keep /alerts as historical alert history.

Do not mix the /alerts history window with /picks current-day behavior.

Verify:

/picks
/picks 20
/picks CS
/picks LOL
/picks MLB
/picks WNBA
and at least one additional Tier 1 sport.

━━━━━━━━━━━━━━━━━━
🔎 FIND THE ACTUAL LIVE CODE PATH
━━━━━━━━━━━━━━━━━━

Do not assume the previous fix is active.

Search the entire current codebase for every implementation of:

* “S-TIER PRIORITY OVERRIDE”
* “Score ≥ 95”
* “score.total >= 95”
* “all validation gates bypassed”
* “high_priority”
* “95”
* “90”
* priority alert routing
* Telegram priority formatting

There must be ONE authoritative priority implementation.

Remove/replace stale duplicate priority logic if it exists.

Do NOT rewrite the architecture.

Do NOT replace the scoring engine.

Do NOT create a second competing priority system.

━━━━━━━━━━━━━━━━━━
🧪 REQUIRED REGRESSION TESTS
━━━━━━━━━━━━━━━━━━

Add/update focused tests for all of these:

1. Raw Score 52 + BQ 95 + MLB OVER
    → Priority if Confidence + Quality pass
2. Raw Score 52 + BQ 95 + MLB UNDER
    → BLOCK
3. Raw Score 52 + BQ 95 + NFL OVER
    → Priority if Confidence + Quality pass
4. Raw Score 52 + BQ 95 + NFL UNDER
    → BLOCK
5. Tier 1 OVER + BQ 95 + Confidence/Quality pass
    → Priority
6. Tier 1 UNDER + BQ 95 + Confidence/Quality pass
    → Priority
7. BQ 94
    → NOT 95+ priority
8. BQ 90–94
    → High Priority, normal applicable gates
9. BQ 95 + Confidence Gate failure
    → BLOCK
10. BQ 95 + Quality Gate failure
    → BLOCK
11. PASS recommendation
    → NEVER actionable
12. Duplicate 95+ scan
    → NO duplicate Telegram alert
13. Reversal of same player/market/game
    → NO spam/conflicting duplicate
14. New legitimate opportunity later
    → Still eligible
15. Telegram priority text
    → Says “Bet Quality 95/100”
    → Does NOT say “Score ≥ 95/100”
16. Old score.total priority path
    → Must not remain active
17. /picks
    → Current-day only
18. /alerts
    → Historical alerts remain available

━━━━━━━━━━━━━━━━━━
🔒 VERSION / FRAMEWORK CHECK
━━━━━━━━━━━━━━━━━━

Before finishing:

Confirm the entire active codebase is using the current:

SHARP MONEY BOT V3.3

Do NOT leave old V3.2 priority logic mixed with V3.3.

Verify:

* Current priority logic
* Current Tier 1/Tier 2 policy
* Current Bet Quality logic
* Current Telegram routing
* Current /picks window
* Current dedup/reversal protection

Do not change working systems that are unrelated.

━━━━━━━━━━━━━━━━━━
🧪 FINAL VALIDATION
━━━━━━━━━━━━━━━━━━

After the targeted fixes:

☐ Focused priority tests
☐ Priority spam/dedup tests
☐ /picks tests
☐ Telegram formatting tests
☐ Full test suite
☐ Startup health check
☐ /status
☐ /health
☐ /funnel 24
☐ /dashboard
☐ /alerts
☐ /picks
☐ /picks 20
☐ /picks CS
☐ /picks LOL
☐ /picks MLB
☐ /picks WNBA
☐ One additional Tier 1 sport
☐ MLB verification
☐ NFL verification

Do NOT manufacture alerts simply to make Telegram appear active.

If no genuinely qualified priority opportunity exists, report that instead.

━━━━━━━━━━━━━━━━━━
📋 FINAL REPORT
━━━━━━━━━━━━━━━━━━

Report:

* Exact files changed
* Exact old priority logic found
* Exact new priority logic
* Exact /picks change
* Exact spam/dedup change
* Focused tests passed
* Full test count
* Any failures
* MLB/NFL status
* Tier 1 status
* 95+ priority status
* 90–94 high-priority status
* Telegram status
* /picks status
* Remaining known issues
* Confirmation everything is running the V3.3 framework

If anything fails:

❌ STOP.

Do not claim completion.

━━━━━━━━━━━━━━━━━━
🧊 FREEZE V3.3
━━━━━━━━━━━━━━━━━━

Only after the targeted fix and validation pass:

🧊 FREEZE SHARP MONEY BOT V3.3

Do NOT:

* Enable sportsbook polling
* Change OddsAPI credentials
* Add providers
* Start another feature pass
* Rewrite anything

This is the final V3.3 priority/picks correction pass.