🔐 CREDENTIALS / SECRETS

Do NOT expose, print, log, echo, or display any credentials, API keys, tokens, secrets, environment variables, or private configuration values.

Keep all credentials completely hidden and use the existing configuration as-is.

━━━━━━━━━━━━━━━━━━
🧊 CURRENT STATE / PHASE
━━━━━━━━━━━━━━━━━━

This is V3.2 Phase 2 Core cleanup after the previous V1.3 stability pass.

We are doing a targeted correction pass.

Do NOT:

* Rewrite the bot
* Rewrite the scoring engine
* Refactor large sections unnecessarily
* Redesign architecture
* Replace working systems
* Change database structure unless absolutely required
* Break the Underdog-first pipeline
* Lower scoring thresholds
* Lower Tier 1 requirements
* Promote all Near-Misses
* Enable OddsAPI polling
* Enable FanDuel
* Enable DraftKings
* Change the OddsAPI key
* Add new providers

Use existing systems wherever possible.

━━━━━━━━━━━━━━━━━━
📌 CURRENT VERIFIED BASELINE
━━━━━━━━━━━━━━━━━━

Full suite after the previous cleanup:

3,675 passed
0 failed

The bot was restarted afterward.

Post-restart verification showed:

* Underdog 🟢 OK
* PropLineHistory: 7,328 and growing
* Background jobs running
* Persistent state survived restart
* Historical recovery correctly labeled historical
* Restart-reason display already removed
* /alerts uses 12-hour AM/PM display
* /alerts says “sent” rather than “delivered”
* Persistent dedup state restoration is intact

Do NOT undo those working fixes.

━━━━━━━━━━━━━━━━━━
🚨 PRIORITY 1 — TIER 1 ACTIONABLE PICK BLOCKER
━━━━━━━━━━━━━━━━━━

This is the highest-priority functional issue.

Recent /funnel 48 output:

Qualified (S/A-tier): 125

Sport breakdown:

MLB: 39 accepted
LOL: 42 accepted
CS: 34 accepted
TENNIS: 1 accepted
MMA: 1 accepted
WNBA: 3 accepted
ESPORTS: 1 accepted
VAL: 4 accepted

But:

/picks
→ No actionable betting picks right now.

/picks CS
→ No qualifying player props available.

/picks LOL
→ No qualifying player props available.

Near-Misses still include:

rx — Kills on Maps 1+2 (CS)
85/100 [S]
qualified

None Quad — Kills on Maps 1+2 (LOL)
83/100 [S]
qualified

CoD: CLEANX — Fantasy Points on Games 1-3 (ESPORTS)
85/100 [S]
decision_pass

This indicates the candidate funnel is accepting Tier 1 candidates while the later /picks/actionable path is rejecting them.

DO NOT assume the cause.

TRACE ONE EXACT CANDIDATE THROUGH THE ENTIRE PATH:

funnel accepted
→ tier assignment
→ validation
→ event validation
→ direction
→ quality/BQ
→ confidence
→ final actionable gate
→ correlation/dedup
→ stored pick creation
→ /picks query
→ Telegram eligibility

Use:

1. rx — CS — Kills on Maps 1+2 — 85 S — qualified

and:

2. None Quad — LOL — Kills on Maps 1+2 — 83 S — qualified

Determine exactly where each one stops.

Also compare them against a known working MLB actionable pick:

Gage Jump — Walks Allowed — OVER 1.5 — S-tier — Bet Quality 95/100

Compare:

WORKING:
MLB → actionable → stored → alert

BROKEN:
Tier 1 CS/LOL → funnel accepted → not actionable

Investigate the ACTUAL code path.

Specifically determine whether Tier 1 sports are being blocked by:

* hidden S-tier requirements
* confidence floor
* Bet Quality requirement
* minimum stars
* validation count
* direction requirement
* event validation
* active/upcoming-game validation
* player/market validation
* correlation
* deduplication
* stale state
* /picks database query
* final actionable query
* sport-specific gate
* incorrect sport mapping
* fallback gate

IMPORTANT:

Do NOT lower any threshold until the exact responsible condition is identified.

Do NOT promote all Tier 1 Near-Misses.

Do NOT modify the scoring engine.

If the code is already enforcing the intended Tier 1 policy, preserve it and fix only the later path that is incorrectly filtering candidates.

━━━━━━━━━━━━━━━━━━
🚨 PRIORITY 2 — /ALERTS COUNT VS ACTUAL TELEGRAM ALERTS
━━━━━━━━━━━━━━━━━━

We previously found:

/alerts:

8 shown (last 72h)
11 all-time sent

The user reports seeing only approximately 1–2 actionable Telegram notifications during the day.

The previous cleanup fixed the counting universe and changed wording from “delivered” to “sent.”

DO NOT undo that.

Verify the complete alert path:

candidate
→ actionable gate
→ broadcast_alert()
→ Telegram send
→ alert_sent=True
→ PropOpportunityLog
→ /alerts count

Determine whether there is still a discrepancy between:

* actual Telegram send attempts
* successful Telegram API responses
* alert_sent=True records
* /alerts displayed count
* all-time sent count

Do NOT invent a delivery-confirmation system.

The system may only claim that Telegram accepted the send request unless an actual message ID/receipt is available.

If the code proves the count is correct, leave it alone.

If a mismatch remains, identify the exact source before changing anything.

━━━━━━━━━━━━━━━━━━
🚨 PRIORITY 3 — /PICKS DISPLAY QUALITY
━━━━━━━━━━━━━━━━━━

A previous bug showed this type of result:

VAL
None Jemkin
Kills on Maps 1+2
Line 31.5
Pick OVER
Confidence 30/100
★☆☆☆☆
Tier —
Higher Difficulty Line

This must NOT appear as an actionable player-prop pick.

The previous cleanup added:

* DB filtering to S/A only
* display confidence floor
* exclusion of unscored/PASS/B rows

Verify that this fix is still working.

Requirements:

* No Tier — picks
* No 1-star/2-star picks
* No low-confidence picks
* No PASS/B-tier props
* No props without valid direction
* No invalid/inactive events
* No season-long futures in player-prop /picks
* Only genuinely actionable candidates should appear

Do NOT lower the existing display/actionable requirements merely to increase the number of picks.

━━━━━━━━━━━━━━━━━━
🚨 PRIORITY 4 — /HEALTH DISPLAY CLEANUP
━━━━━━━━━━━━━━━━━━

Remove ONLY these display lines from /health:

* Previous session:
* Crash detected:

IMPORTANT:

Do NOT remove or modify the underlying:

* crash detection
* restart tracking
* health tracking
* recovery tracking
* persistent state
* restart-survival logic

The purpose is only to remove confusing display noise.

Preserve:

* Uptime
* Heartbeat
* Last startup
* Background job health
* Provider health
* Pipeline health
* Historical recovery handling

━━━━━━━━━━━━━━━━━━
💾 PERSISTENT MEMORY — DO NOT REGRESS
━━━━━━━━━━━━━━━━━━

The bot must resume from existing persistent state after restart.

Important state must NOT reset when the bot restarts.

Verify and preserve:

* PropLineHistory
* Stored actionable picks
* Pending picks
* Qualification history
* Scoring history
* Hit/loss history
* Resolution state
* CLV seeds/history
* Alert history
* Alert dedup state

Use the existing database/storage.

Do NOT create a new database unless absolutely necessary.

A restart should resume the bot’s existing progress, not rebuild/reset its intelligence.

━━━━━━━━━━━━━━━━━━
🧪 TESTING REQUIREMENTS
━━━━━━━━━━━━━━━━━━

Before modifying code:

1. Review the relevant implementation.
2. Identify the exact root cause for each confirmed issue.
3. Make targeted changes only.
4. Add focused regression tests.

Then run:

* Focused tests
* Full test suite
* Startup health check
* Restart/persistence verification if needed
* /picks verification
* /funnel verification
* /alerts verification
* /health verification

Do not make unrelated changes.

━━━━━━━━━━━━━━━━━━
🚫 PHASE 2 BOUNDARY
━━━━━━━━━━━━━━━━━━

DO NOT touch:

* OddsAPI polling
* FanDuel
* DraftKings
* sportsbook alerts
* Moneyline sportsbook polling
* Totals sportsbook polling
* Spread sportsbook polling
* OddsAPI credentials/key
* New providers

Those remain a separate future stage.

━━━━━━━━━━━━━━━━━━
📋 FINAL REPORT
━━━━━━━━━━━━━━━━━━

After the fixes and tests, report:

1. Exact root cause of Tier 1 CS/LOL actionable blocking
2. Exact function/module/condition responsible
3. Whether Tier 1 policy itself was wrong or whether a later gate was wrong
4. Exact /alerts count discrepancy status
5. Exact /picks filtering issue status
6. /health display cleanup status
7. Persistence/restart-survival status
8. Files changed
9. Focused tests passed
10. Full test suite result
11. Startup health result
12. Remaining known issues

Do NOT simply say “done.”

For every issue explicitly state:

✅ VERIFIED

or

❌ FAILED

with the reason.

If any test fails, STOP and investigate it before declaring the phase complete.

Do not make unrelated improvements.
Do not add features.
Do not change thresholds without evidence.
Do not enable sportsbook polling.
Do not expose credentials.

━━━━━━━━━━━━━━━━━━
🧊 FINAL FREEZE
━━━━━━━━━━━━━━━━━━

Once all requested fixes are verified and the full test suite passes:

FREEZE V3.2 PHASE 2 CORE.

Do not begin the separate sportsbook stage.

Do not make additional changes after the freeze unless a new, confirmed defect is identified.