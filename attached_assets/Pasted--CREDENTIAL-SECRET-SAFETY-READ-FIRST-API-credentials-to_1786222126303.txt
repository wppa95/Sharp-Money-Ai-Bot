🔒 CREDENTIAL / SECRET SAFETY — READ FIRST

API credentials, tokens, keys, secrets, and environment variables must NEVER be exposed in:

* Logs
* Telegram
* /status
* /health
* /config
* Diagnostics
* Test output
* Error messages
* Screenshots/output pasted into reports

Do not print, echo, dump, inspect, or reveal secret values.

If a credential must be verified, verify only that it is PRESENT/CONFIGURED and that the connection works.

The user will manually change any secrets in Replit Secrets when instructed. Never ask the user to paste a secret into ChatGPT.

━━━━━━━━━━━━━━━━━━

Before making any changes, review the current V3.2 Phase 2 Core state.

We completed the command audit after a clean manual restart.

DO NOT change:

* Scoring thresholds
* Tier thresholds
* Qualification logic
* Underdog pipeline
* /picks logic
* /alerts logic
* Funnel logic
* Dashboard aggregation
* Database structure
* Telegram behavior
* OddsAPI
* FanDuel
* DraftKings

We found ONE remaining cleanup issue:

/health — STALE HISTORICAL RECOVERY DISPLAY

Current /health repeatedly shows:

Last recovery: ~61h ago · job: underdog_job
↳ fail_streak=3: ‘list’ object has no attribute ‘has_real_data’

This is an OLD Aug 6 historical failure.

The CURRENT bot session is healthy:

* Heartbeat is current
* Underdog monitor is running
* Underdog fetch is current
* Pipeline scans are running
* Database writes are current
* No new has_real_data failure is occurring

The current-session startup reason may remain:

Restart reason: Unexpected exit (SIGKILL / OOM / hard crash)

That is separate from the stale historical recovery.

TASK

Fix ONLY the /health presentation/state handling so an old recovery is not presented as the current active recovery/failure.

Requirements:

1. Review the existing HealthTracker / /health implementation first.
2. Do not rewrite the health system.
3. Do not delete historical database records.
4. Do not change the current-session restart reason.
5. Do not change pipeline behavior.
6. Do not change scoring or qualification.
7. Do not change alert behavior.
8. Do not expose credentials.
9. Follow the existing stale-state handling already used for other health fields.
10. A recovery/failure this old should either:

* be suppressed from the active /health section, OR
* be explicitly labeled historical rather than presented as the current recovery.

11. A genuinely recent recovery must still display correctly.
12. /restarts must remain removed.

TESTS

Add focused tests covering:

* Old/stale recovery is not presented as current.
* Recent recovery still displays.
* Current-session restart reason still displays.
* Current heartbeat/job health remains unaffected.
* Existing /health behavior does not regress.

Run:

1. Focused health tests.
2. Full test suite.

STOP after tests.

FINAL FREEZE

ONLY if all required tests pass:

* Freeze V3.2 Phase 2 Core.
* Do not make additional changes.
* Do not add features.
* Do not change thresholds.
* Do not enable sportsbook polling.
* Do not enable FanDuel.
* Do not enable DraftKings.
* Do not change the OddsAPI key.
* Do not modify the Underdog-first architecture.

Final report must contain:

* Files changed
* Focused tests passed
* Full suite result
* Exact /health behavior change
* Confirmation credentials/secrets were protected
* Confirmation no scoring/qualification changes were made
* Confirmation no Underdog pipeline changes were made
* Confirmation no Telegram behavior changes were made
* Remaining known issues

Then explicitly state:

🧊 V3.2 PHASE 2 CORE — FROZEN