⚠️ REPLIT CREDENTIAL WARNING

For this Replit bot task, protect the configured credentials/secrets. Do not waste, repeatedly test, expose, rotate, or unnecessarily consume credentials while working on unrelated tasks. Avoid unnecessary credential lookups or one-by-one credential checks. Reuse existing configured credentials when needed and only access them when the current task actually requires them.

CREDENTIAL-SAFETY CHECKLIST

Before starting:

* Do not expose or print credentials/secrets.
* Do not rotate/delete/reconfigure credentials unless specifically required.
* Do not repeatedly test credentials when the current task does not require it.
* Do not waste time checking credentials one-by-one.
* Reuse existing configured credentials when required.
* Keep credential work strictly limited to what is necessary for the Replit bot task.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚨 MASTER BUG-FIX / VALIDATION TASK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

We have identified multiple problems during live testing.

Do NOT fix only the latest symptom. Audit the entire candidate → qualification → queue → dedup → Telegram delivery pipeline and fix ALL problems below together.

Do NOT perform unrelated refactoring.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1️⃣ DUPLICATE TELEGRAM DELIVERY — CRITICAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The exact same Brandon Nimmo MLB Hits OVER 0.5 candidate was delivered repeatedly.

Observed alerts were essentially identical:

* Brandon Nimmo
* MLB
* Hits
* OVER 0.5
* Previous line 1.5
* Move -1.0
* BQ 85
* MQ 90
* S-Tier
* Same underlying analysis
* Only timestamp changed

It was delivered at approximately:

* 6:43 PM ET
* 6:45 PM ET
* 6:47 PM ET
* 6:49 PM ET

This proves the existing delivery deduplication is NOT sufficient.

REQUIRED FIX

Create/enforce ONE centralized candidate-level Telegram delivery deduplication mechanism.

Search the entire repository for every:
deliver_underdog()

caller.

Every Telegram delivery path must use the same deduplication protection.

Candidate identity must NOT contain timestamp.

Use sufficient identity fields to distinguish an actual new candidate/state, such as:

provider + sport + player + market/stat + line + direction

If the exact same candidate remains available and unchanged:

→ SEND ONCE ONLY.

Repeated scan:
→ NO SEND.

Stable refresh:
→ NO SEND.

Watchlist:
→ NO SEND.

FPR/full-pool:
→ NO SEND.

New-prop/line-change/standing:
→ NO SEND if it is the same active candidate.

Multiple async paths:
→ only ONE delivery may succeed.

CONCURRENCY REQUIREMENT

The check-and-record operation must be atomic.

Two async jobs must NOT both pass the dedup check before either records the delivery.

NEW STATE REQUIREMENT

A genuinely new candidate state must still be able to alert.

Examples:

* Genuine line/state change.
* Prop disappears and later returns as a genuinely new active state.
* Previously rejected/watchlisted candidate becomes genuinely actionable.

Do NOT simply reset deduplication every scan.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2️⃣ TIER DEFINITIONS — MUST REMAIN STRICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🥇 TIER 1 — PRIORITY

Tier 1 = EVERY supported sport EXCEPT NBA, MLB, and NFL.

Examples include:
BADMINTON, CS/CS2, DOTA/DOTA2, GOLF, LOL, MMA, NCAAF/CFB, NHL, SOCCER/FIFA, TABLE TENNIS/TT, TENNIS, VALORANT/VAL, WNBA, RACING, CFL, MOTORCYCLE, OUTLAWS, and any other supported sport that is NOT NBA, MLB, or NFL.

Tier 1 rules:

* Valid OVER or UNDER direction required.
* Strong/actionable analysis required.
* NO standalone BQ >=85 requirement.
* NO standalone MQ >=85 requirement.
* Do NOT reject Tier 1 merely because BQ is below 85.
* Do NOT reject Tier 1 merely because MQ is below 85.
* Tier 1 has priority over Tier 2.

In simple terms:

Tier 1 = direction + strong/actionable analysis.

🥈 TIER 2 — ONLY NBA / MLB / NFL

Tier 2 = ONLY:

* NBA
* MLB
* NFL

Tier 2 rules:

* Valid OVER or UNDER direction required.
* BQ >=85.
* MQ >=85.
* BOTH are mandatory for Telegram delivery.

Examples:

MLB BQ 85 / MQ 90 → ✅ SEND
MLB BQ 90 / MQ 85 → ✅ SEND
MLB BQ 84 / MQ 95 → ❌ BLOCK
MLB BQ 95 / MQ 84 → ❌ BLOCK
NFL BQ 93 / MQ 62 → ❌ BLOCK
NBA BQ 88 / MQ 88 → ✅ SEND

Tier 2 props that fail the gate must STILL be continuously scanned, scored, and retained internally.

Only Telegram delivery is blocked.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3️⃣ REMOVE THE OLD 8/2 SYSTEM COMPLETELY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The old:

* 8 Tier 1
* 2 Tier 2

allocation is removed.

There are NO separate Tier 1/Tier 2 delivery caps.

Keep only:

10 total Telegram alerts maximum per 5-minute window.

Tier 1 has priority when candidates compete for available slots.

Search the entire repository for remaining:

* _DQ_TIER1_CAP
* _DQ_TIER2_CAP
* _dq_t1_delivered
* _dq_t2_delivered
* any equivalent 8/2 logic

There should be ZERO active 8/2 enforcement.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4️⃣ TIER 1 IS BEING STARVED / NOT REACHING TELEGRAM
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After the Tier 1/Tier 2 changes, valid Tier 2 picks have appeared, but we have seen essentially no Tier 1 Telegram picks.

Do NOT simply loosen Tier 2.

Find exactly where Tier 1 candidates disappear.

Audit:

* sport classification
* scoring
* qualification
* direction gate
* strong/actionable analysis gate
* BQ/MQ gates
* queue insertion
* queue ranking
* diversification
* delivery backstop
* stable refresh
* watchlist
* FPR
* rate limiter
* deduplication

Tier 1 must be ranked ahead of Tier 2.

BUT Tier 1 must NOT be completely starved by queue/rate-limit behavior.

REQUIRED FUNNEL

Add/report Tier 1 vs Tier 2 counts for:

* fetched
* active
* scored
* qualified
* passed tier gate
* queued
* rejected
* deduped
* rate-limited
* delivered

We need to know EXACTLY where Tier 1 candidates are disappearing.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5️⃣ PREVIOUS MQ BYPASS PATHS — VERIFY THEY STAY FIXED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

We previously discovered MQ-gate bypasses in:

* stable refresh
* watchlist
* full-pool/FPR

These paths directly called Telegram delivery and bypassed the main delivery queue.

Verify that the current _tier_delivery_gate() is actually applied to ALL of them.

Do NOT assume the previous fix covers them.

Audit every direct delivery path.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6️⃣ /PICKS AND /SLIP MUST MATCH DELIVERY ELIGIBILITY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

/picks and /slip must not show Tier 2 NBA/MLB/NFL props as actionable when:

* BQ <85 OR
* MQ <85

Tier 1 remains eligible based on:

* valid direction
* strong/actionable analysis

The command logic must use the same canonical tier rules as Telegram delivery.

No competing thresholds.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7️⃣ SINGLE SOURCE OF TRUTH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

There must be ONE canonical sport classifier:

_TIER2_SPORTS = {"NBA", "MLB", "NFL"}

Tier 1 is the complement.

There must be ONE canonical delivery gate:

Tier 1:
direction + strong/actionable analysis

Tier 2:
direction + BQ >=85 + MQ >=85

Do not duplicate different versions of this logic across:

* new-prop
* line-change
* standing
* delivery queue
* stable refresh
* watchlist
* FPR
* /picks
* /slip

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8️⃣ CONTINUOUS SCANNING MUST REMAIN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Do NOT stop monitoring a prop because it was:

* rejected
* below Tier 2 threshold
* deduplicated
* rate-limited
* not currently actionable

The bot should continue scanning and storing the data.

Telegram delivery eligibility is separate from monitoring.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9️⃣ TELEGRAM RATE LIMIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Keep:

10 total Telegram alerts / 5-minute window.

No 8/2 split.

Tier 1 gets priority in candidate ranking.

Verify the rate limiter cannot accidentally starve Tier 1 because Tier 2 candidates were processed first.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔟 REGRESSION TESTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Add/run tests for all of the following.

Duplicate delivery

* Same candidate discovered twice → 1 delivery.
* Same candidate discovered by two delivery paths → 1 delivery.
* Stable refresh duplicate → 1 delivery.
* Watchlist duplicate → 1 delivery.
* FPR duplicate → 1 delivery.
* Concurrent duplicate attempts → 1 delivery.
* Genuine line/state change → new alert permitted.
* Disappeared/reappeared candidate can alert as a genuinely new state.

Tier 1

* Tier 1 OVER with BQ <85 / MQ <85 → allowed if strong/actionable analysis passes.
* Tier 1 UNDER with BQ <85 / MQ <85 → allowed if strong/actionable analysis passes.
* Tier 1 is prioritized over Tier 2.
* Tier 1 is not subject to the Tier 2 85/85 gate.

Tier 2

* NBA/MLB/NFL BQ 85 / MQ 85 → allowed.
* BQ 85 / MQ 90 → allowed.
* BQ 90 / MQ 85 → allowed.
* BQ 84 / MQ 90 → blocked.
* BQ 90 / MQ 84 → blocked.
* BQ 84 / MQ 84 → blocked.
* 84.99 → blocked.
* 85.00 → allowed.
* Only NBA, MLB, NFL are Tier 2.

Known bad examples

* MLB UNDER BQ 73 / MQ 47 → blocked.
* NFL UNDER BQ 93 / MQ 62 → blocked.
* MLB OVER BQ 85 / MQ 90 → allowed.

Commands

* /picks respects the same Tier 2 85/85 rule.
* /slip respects the same Tier 2 85/85 rule.
* Tier 1 commands do not impose the Tier 2 numeric gate.

Rate limit

* Maximum 10 Telegram alerts per 5 minutes.
* No 8/2 enforcement.
* Tier 1 priority is preserved.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔎 FINAL AUDIT CHECKLIST — DO NOT RESTART UNTIL COMPLETE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before restarting the bot, check every item and report the result:

* Every deliver_underdog() caller identified.
* Every Telegram delivery path uses centralized deduplication.
* Deduplication is atomic/concurrency-safe.
* Duplicate Nimmo scenario is covered by regression test.
* Tier 1 classification verified.
* Tier 2 classification verified.
* Tier 1 has no BQ/MQ 85 numeric blocker.
* Tier 2 requires BQ >=85 AND MQ >=85.
* Stable-refresh gate verified.
* Watchlist gate verified.
* FPR gate verified.
* /picks gate verified.
* /slip gate verified.
* Tier 1 priority verified.
* Tier 1 starvation/root cause identified.
* Global 10/5-minute limit verified.
* All 8/2 references removed.
* Continuous scanning preserved.
* No unrelated refactoring performed.
* Focused tests pass.
* FULL test suite passes.
* Changes committed.

FINAL CONFIRMATION REQUIRED

After completing everything, provide ONE final report containing:

1. Root cause of duplicate Telegram delivery.
2. Root cause of Tier 1 not reaching Telegram.
3. Every delivery path fixed.
4. Exact files changed.
5. Focused test count/result.
6. Full test count/result.
7. Confirmation that Tier 1/Tier 2 rules are correct.
8. Confirmation that 8/2 is completely removed.
9. Confirmation that 10 alerts / 5 minutes remains.
10. Confirmation that continuous scanning remains.
11. Confirmation that /picks and /slip match the same eligibility rules.
12. Git commit hash.
13. Whether a restart is required.

DO NOT restart until the audit and tests are complete.